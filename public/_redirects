# SPA fallback: serve the app shell for client-side routes.
# Static assets are served directly, and /api/* is handled by the Pages Function
# (functions/api/[[path]].js), both of which take precedence over this rule.
/*    /index.html    200
